# Knowledge

Derived docs and memory that help agents orient without rereading all history.

- `docs/` — harness documentation, core beliefs, quality rubrics
- `memory/` — continuously refreshed topic memory
- `state/` — repo-aware bootstrap state (profile, inventory, module map)
