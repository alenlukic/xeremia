---
run_id: 20260406T013033Z
mode: unknown
published_at: 2026-04-06T02:30:35.678525+00:00
qa_verdict: PASS
build_status: PASS
breaker_verdict: UNKNOWN
eval_verdict: PASS
eval_score: 80
regression_severity: UNKNOWN
---
# Run Ledger

## Outcome
- Task: UI border/divider layout (Browse + Matches), 8px gap above tables, left-aligned tab labels, and weight-gauge tooltip stacking/position/transparency (R1–R7).
- Result: PASS — evaluator met threshold (80 / B-); QA, build, and specific/broad review APPROVE; no retry rounds (`RETRY_LOG.jsonl` empty).
- Scope: `client/src/styles.css`, `client/src/App.tsx`, `client/src/components/TrackTable.tsx` (task also named `MatchesPanel`; framing applied via `App` wrappers only).

## Key Decisions
- Introduced `.table-panel` (flex column, `margin: 0 var(--content-gutter)`, left/right `1px` borders, `overflow: hidden`) so only the tab-bar bottom border is full viewport width; inner chrome sits between vertical borders (R2–R4).
- Replaced inner `var(--content-gutter)` horizontal padding on filter bar, bucket tabs, and panel title with fixed `14px` so horizontal dividers align with the panel interior (R3).
- Removed `isFramed` / `track-table-wrapper--framed`; table width and gutters are unified through `.table-panel` and `TrackTable` measuring the wrapper’s `clientWidth`.
- R7 addressed with CSS-only `::after` tooltips: position above the label (`bottom: calc(100% + 6px)`), `z-index: 9999`, semi-opaque `rgba(44, 49, 66, 0.82)` background.
- Added unrequested `useLayoutEffect` + `ResizeObserver` in `App.tsx` to drive `searchPadding` into `SearchPanel` for gauge-row alignment (reviewers flagged as scope creep; `SearchPanel` already accepted the prop).

## Verification Learnings
- Live stack checks: Elasticsearch search, match load, cache stats, and key APIs returned 200; Browse/Matches layout and gaps validated in browser per QA.
- Client build (`npm run build`) passed with no TS errors; change is CSS/layout-only on the backend dimension.
- Evaluator marked **tests** dimension MISSING and **breaker/regression** UNKNOWN; ledger still reflects explicit breaker and regression markdown reports.
- R7 is difficult to prove with automation: QA noted hover/`::after` limits and relied on static CSS verification alongside layout checks.

## Breaker / Regression Learnings
- Breaker verdict **CONCERNS** (not treated as layout blocker): `.app-shell-v2 { overflow: hidden }` can clip a tooltip that extends above the label; high `z-index` does not escape `backdrop-filter` stacking contexts on `.gauge-group` / `.fusion-pane` — acceptable until overlapping top chrome appears.
- Breaker noted dependency `[weightsLoading]` on the search-padding effect may miss updates if layout changes without resize or that flag toggling (nit on out-of-scope code).
- Regression report: **LOW** severity, no confirmed unintended drift outside R1–R7; `isFramed` and `.track-table-wrapper--framed` fully removed without orphan references.

## Durable Repo Guidance
- When horizontal gutters move from inner wrappers to an outer framed panel, re-derive table `ResizeObserver` width from the new parent; drop obsolete “framed” margin conditionals tied to old measurement thresholds.
- Pseudo-element tooltips inherit ancestor `overflow` and local stacking contexts; if “always on top” must hold against global UI layers, plan for a portal or overflow-safe placement rather than only raising `z-index`.
- Keep delivery scope aligned with TASK “Files in Scope”: coupling `App` measurement to `SearchPanel` without a task line item invites review nits and eval noise.
- Repeated literal `14px` insets could become a single token if design tweaks recur.

## Deferred / Follow-up
- TASK out-of-scope items unchanged: no backend/API work; `MatchDetail` and `WeightControls` logic untouched by requirement list.
- Optional: manual tooltip checks under small viewports / future top chrome; consider a shared inset token if `14px` repeats elsewhere.
