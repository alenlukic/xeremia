---
run_id: 20260407T115052Z
mode: unknown
published_at: 2026-04-07T12:41:01.470864+00:00
qa_verdict: PASS
build_status: CONDITIONAL
breaker_verdict: UNKNOWN
eval_verdict: PASS
eval_score: 87
regression_severity: LOW
---
# Run Ledger

## Outcome
- Task: Migrate `MatchesPanel` to the Browse-style TanStack table interaction model with the exact nine factor columns and preserved bucket behavior.
- Result: `MatchesPanel` now uses the Browse-style resize/reorder/scroll interaction model with the contract column order and mappings, and the retry fixed the first-mount overflow measurement bug so the top scrollbar renders after a track is first selected.
- Scope: Client-only UI, test, and supporting tooling changes around `MatchesPanel`; repo-wide Python structure-test failures remained outside the diff.

## Key Decisions
- Decision: Rebuild the Matches grid on the same TanStack interaction pattern used by Browse instead of extending the previous bespoke matches table.
  - Why: This satisfied the contract's exact interaction requirements and kept the branch stable enough for downstream work to branch from it.
  - Tradeoff: Some interactive-table logic is now duplicated between `MatchesPanel` and `TrackTable`, which is a maintainability cost but was accepted to keep this contract narrow.
- Decision: Fix the overflow/measurement defect with a `selectedTrack`-aware layout-effect rerun instead of adding extra scroll or sizing heuristics.
  - Why: The real bug was that the measured container did not exist on initial mount, so overflow state never initialized after first selection.
  - Tradeoff: The fix is local to `MatchesPanel`'s conditional-render pattern rather than a generalized table utility.

## Verification Learnings
- Interactive table behavior needed browser-level evidence; unit tests alone were not enough to prove drag-reorder, resize, alignment, or dual-scrollbar sync.
- Live-stack QA was still required even after mocked Playwright coverage, because the contract also depended on real search, match loading, cache population, and error-free normal API flow.
- A repo-wide baseline failure can keep build verification at `CONDITIONAL` even when all in-scope checks pass; in this run the unrelated Python layer-direction test blocked an unconditional repo-wide build `PASS`.

## Product / Stakeholder Learnings
- The Matches view can be reduced to the nine factor columns without losing bucket workflow value; users still get practical comparison behavior from factor-by-factor scanning plus bucket counts.
- Removing the old score-click path leaves match-detail discoverability intentionally deferred; any follow-on detail entrypoint should be introduced explicitly rather than inferred from the old grid behavior.

## Durable Repo Guidance
- Scope: subsystem-specific
  - Guidance: If a responsive table's measurement target is conditionally rendered, mount-only sizing effects are not sufficient; remeasure on the state transition that makes the container exist.
- Scope: subsystem-specific
  - Guidance: For Browse-style TanStack tables, keep a focused runtime suite that covers exact header order, drag-reorder, resize width change, header/body alignment, and overflow-scroll sync.
- Scope: one-off
  - Guidance: Treat the current `src/tests/test_structure.py::test_layer_dependency_direction` failure as a pre-existing repo baseline issue, not as evidence against client-only contract branches.

## Deferred / Follow-up
- Contract 4 should keep smoke coverage on the next Matches-to-detail entrypoint, because this branch intentionally removed the previous score-click path.
- Revisit shared extraction between `MatchesPanel` and `TrackTable` only if another contract needs the same interaction chrome; it was not worth broadening scope here.
