# Spec Layer

Static agent, command, and rule definitions. These define behavior but do not represent live work.

- `agents/` — canonical agent prompt contracts
- `commands/` — operator-facing command prompts and workflow entrypoints
- `MODEL_COMPATIBILITY.md` — model compatibility notes
