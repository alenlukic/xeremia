---
run_id: 20260407T124201Z
mode: delivery
published_at: 2026-04-07T13:13:33.355245+00:00
qa_verdict: PASS
build_status: CONDITIONAL
breaker_verdict: UNKNOWN
eval_verdict: FAIL
eval_score: 50
regression_severity: NONE
---
# Run Ledger

## Outcome
- Task: Contract 5 `fusion-weight-scoring-integration` wired persisted fusion subweights into live late-fusion scoring.
- Result: `src/feature_extraction/track_similarity.py` now normalizes live fusion weights before combining harmonic, rhythm, timbre, and energy similarity, making persisted fusion subweights materially affect production similarity and transition scores while keeping outputs finite and bounded.
- Scope: Narrow production change in `src/feature_extraction/track_similarity.py` plus targeted proof in `src/tests/test_track_similarity.py` and `src/tests/test_transition_match.py`; no new API surface, persisted keys, or cache behavior changes.

## Key Decisions
- Decision: Normalize the live fusion weights at scoring time instead of adding a parallel weighting path or new persisted fields.
  - Why: Persisted subweights were effectively inert or could distort late-fusion output when they did not sum to `1.0`, and the existing backend flow already exposed the right runtime source of truth.
  - Tradeoff: This keeps the production patch very small and backward-compatible for default weights, but it leaves invalid negative inputs as a separate boundary-validation concern rather than solving that in Contract 5.
- Decision: Prove the contract with end-to-end score-change evidence, not just unit assertions on stored values.
  - Why: The real requirement was that weight changes alter live similarity-driven scoring and match ordering, not merely that weights round-trip through storage.
  - Tradeoff: Verification stayed focused on targeted backend tests plus live API/cache checks instead of attempting a broader scoring architecture refactor.

## Verification Learnings
- Targeted tests now prove changing fusion weights changes `late_fusion_v1` and `TransitionMatch` outputs while keeping similarity in `[0,1]` and total scores bounded/finite, including the all-zero fusion-weight edge case.
- Live QA confirmed `GET /api/weights` and `PUT /api/weights` still round-trip persisted fusion values, cache invalidation still clears and repopulates correctly, and changing weights produces visibly different live match ordering and scores.
- The repository-wide build remains non-green because of the pre-existing structural test failure `src/tests/test_structure.py::test_layer_dependency_direction` about `feature_extraction/track_similarity.py` importing `harmonic_mixing`; this run did not introduce that failure.

## Product / Stakeholder Learnings
- Fusion-weight tuning now has observable customer-facing impact: changing persisted subweights changes returned matches and score ordering in the live system, giving end-to-end evidence that the weight controls are no longer inert.
- The most important confidence signal for future scoring contracts is live behavior, not just configuration persistence, because silent no-op weights can look healthy until rankings are inspected.

## Durable Repo Guidance
- Scope: subsystem-specific
  - Guidance: When persisted scoring weights are consumed in late-fusion math, normalize them at the scoring boundary so proportional intent survives storage/API scaling and score ranges remain stable.
- Scope: subsystem-specific
  - Guidance: For scoring-weight work, require tests that prove both propagation into downstream match scoring and bounded numeric behavior; storage round-trip alone is insufficient evidence.
- Scope: one-off
  - Guidance: Treat the `test_layer_dependency_direction` failure involving `track_similarity.py` and `harmonic_mixing` as pre-existing structural debt unless a future contract actually changes that import edge.

## Deferred / Follow-up
- Breaker found no blocker or important issues; only watch items remain around rejecting negative fusion-weight inputs at the API/service boundary and adding explicit coverage for negative harmonic-similarity cases.
