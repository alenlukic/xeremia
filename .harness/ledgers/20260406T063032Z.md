---
run_id: 20260406T063032Z
mode: delivery
published_at: 2026-04-06T06:51:09.594540+00:00
qa_verdict: PASS
build_status: PASS
breaker_verdict: PASS
eval_verdict: PASS
eval_score: 100
regression_severity: NONE
---
# Run Ledger

## Outcome
- Task: Fix lossless-to-AIFF conversion emitting Windows-unreadable AIFF-C float outputs.
- Result: PASS.
- Scope: Updated AIFF subtype selection/docstring in `src/track_metadata/utils.py`, added targeted regression coverage in `src/tests/track_metadata/test_utils.py`, and fixed a harness path-check false positive in `.harness/bin/pipeline.py`.

## Key Decisions
- Replaced format-check-only subtype preservation with a strict PCM allowlist gate (`PCM_S8`, `PCM_16`, `PCM_24`, `PCM_32`) plus `sf.check_format("AIFF", subtype)` to prevent non-PCM passthrough.
- Chose `PCM_24` fallback for non-allowlisted source subtypes (including FLOAT/DOUBLE) instead of `PCM_16` to preserve higher fidelity while restoring Windows compatibility.
- Kept behavior unchanged for supported PCM inputs by preserving allowlisted subtypes when AIFF-valid.
- Added a focused regression test that asserts output subtype metadata (`PCM_24`) rather than only conversion success/file existence.

## Verification Learnings
- Targeted conversion tests passed (12/12), including explicit FLOAT WAV -> AIFF `PCM_24` coverage.
- Review concluded the patch is narrow, complete, and non-regressive for existing PCM-preservation behavior (APPROVE, no findings).
- QA, build, and policy gates all passed; evaluator score is 100 (grade A, verdict PASS).
- The subtype assertion in tests is the critical check; existence-only assertions are insufficient for codec-compatibility regressions.

## Breaker / Regression Learnings
- Breaker attempts found no bypass of the new guard: FLOAT/DOUBLE and companded/unsupported subtypes all route to `PCM_24`.
- No blocker/important issues were found; only nits around adding an explicit DOUBLE fixture path and clarifying test helper defaults.
- Regression detector outcome remained NONE, indicating no adjacent behavior drift surfaced from the scoped change.

## Durable Repo Guidance
- Treat `sf.check_format` as necessary but not sufficient for cross-platform compatibility decisions; pair capability checks with explicit subtype policy.
- Encode codec-compatibility intent as allowlists for format conversion boundaries, and default non-conforming inputs to a safe canonical subtype.
- For media-conversion regressions, assert output metadata (container/subtype/sample characteristics) directly in tests.
- Keep harness path checks explicit (path-segment aware) to avoid substring false positives in pipeline gating.

## Deferred / Follow-up
- Optional: add explicit DOUBLE-input regression coverage (same fallback path, currently unblocked).
- Optional: document test helper WAV subtype defaults to reduce future confusion in conversion tests.
- None required for merge readiness.
