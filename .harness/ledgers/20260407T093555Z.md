---
run_id: 20260407T093555Z
mode: delivery
published_at: 2026-04-07T10:06:45.443150+00:00
qa_verdict: FAIL
build_status: FAIL
breaker_verdict: CONCERNS
eval_verdict: FAIL
eval_score: 23
regression_severity: NONE
---
# Run Ledger

## Outcome
- Task: Surface client-side match and browse load failures instead of collapsing failed requests into normal empty states.
- Result: Scoped client changes met the feature contract and reviewer approved them, but the run stopped with `QA=FAIL`, `Build Verification=FAIL`, and `Evaluation=FAIL` (`23`, below threshold `80`).
- Scope: Local client-only failure-state plumbing in `useSelectedTrack`, `useCollectionCache`, `App`, `MatchesPanel`, `TrackTable`, `MatchDetail`, related tests, and a small style addition; no backend or API-contract changes.

## Key Decisions
- Decision: Preserve explicit per-surface error state in existing hooks/components instead of introducing a shared cross-app error model.
  - Why: The contract required local changes that distinguish failed loads from successful zero-result responses without backend changes.
  - Tradeoff: Error handling remains surface-specific and does not add retry orchestration or global recovery UX.
- Decision: Treat breaker-raised `IMPORTANT` findings as a separate follow-on delivery run instead of folding them back into this run.
  - Why: That matches repo policy for adversarial findings and preserves auditability once a real diff has already been reviewed and verified.
  - Tradeoff: The original run closes with unmet gates even though the scoped UI behavior largely landed.

## Verification Learnings
- Focused frontend evidence was strong: the reviewer marked requirement fit as fully met and the targeted client suite passed (`16/16`), including failure-vs-empty-state coverage for matches and browse surfaces.
- Full verification still blocked completion: broad pytest failed on an unrelated structural dependency test, and live QA gates were not clean because service lifecycle was not fully revalidated and runtime logs contained `4XX/5XX` entries.
- The highest-confidence remaining gap was not the new browse/matches copy but the missing proof around `MatchDetail` failure rendering and the stale `matchesError` lifecycle on refetch.
- Regression review found no unintended drift outside the scoped client surfaces, so the stop condition came from quality gates rather than scope expansion.

## Product / Stakeholder Learnings
- Distinguishing "request failed" from "request succeeded with no results" is important on both Browse and Matches; collapsing failures into empty-state copy created misleading UX.
- Reusing the existing local failure surfaces was sufficient for this slice; broader recovery UX such as retry affordances was explicitly deferred.

## Durable Repo Guidance
- Scope: subsystem-specific
  - Guidance: When adding UI failure handling in this client, preserve explicit error state through the hook/component boundary rather than normalizing failures to empty collections.
- Scope: subsystem-specific
  - Guidance: For data-table surfaces, keep error copy and zero-result copy as separate branches and test both paths explicitly.
- Scope: repo-wide
  - Guidance: A scoped feature run can still close as failed if live QA gates or full verification remain red; capture the shipped behavior separately from the stop condition.
- Scope: repo-wide
  - Guidance: Breaker `IMPORTANT` findings should become a follow-on contract/run when they are narrow and actionable, rather than being silently absorbed into the original run.

## Deferred / Follow-up
- Follow-on status: Created `BREAKER_FOLLOW_ON_CONTRACT.md` and recorded follow-on child run `20260407T100501Z`.
- Follow-on scope: clear `matchesError` at the start of every new `loadMatches` request and add focused coverage for that lifecycle; add a focused automated test for rejected `MatchDetail` fetches.
- Deferred: browse retry/recovery UX and CSS error-token cleanup were intentionally left out of the follow-on because they were watch-level concerns, not blocker-level fixes.
