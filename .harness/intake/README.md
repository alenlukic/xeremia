# Intake

The canonical landing zone for ongoing context ingest and routing.

- `sources/` — ingest source directories (dropbox, processing, processed, rejected)
- `INDEX.md` / `INDEX.json` — current intake state
- `ROUTING_EVENTS.jsonl` — event log of routing decisions
