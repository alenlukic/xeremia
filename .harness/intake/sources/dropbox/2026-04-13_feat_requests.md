# STATUS: READY FOR INGEST

- [!!P0!!] client -> audio player: keep seeing "Failed to load because no supported source was found." for many files (e.g. [03B - Db - 096.00] Emancipator - Siren (Tor Remix))
- client -> set: tracks from Tracklist or Pool should be draggable to Explorer (same as from track table or match table)
- client -> explorer: when a track is dragged to explorer, moving it to the top or bottom of the explorer area should trigger scroll up or down the tree (currently does not)
- client -> explorer: nodes should be draggable from their current position to any open position on the tree, or as a child of another node in the tree
- client -> all tables: enable multi-sort (e.g. first by key, then by bpm)
- client -> track table: when playing a track, the row should be highlighted for visual clarity
- client -> track table: make the area around the playback button not trigger track selection (which filters the table on that track) - too easy to trigger by accident right now
- client -> track table: if track is selected and then search bar is cleared, remember the user's last browse position in the table

