# Intake Sources

Ingest source directories for context routing.

- `dropbox/` — new items awaiting classification
- `processing/` — items currently being processed
- `processed/` — successfully processed items
- `rejected/` — items that failed validation or were not applicable
