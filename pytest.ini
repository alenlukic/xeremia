[pytest]
markers =
    integration: tests requiring external services, live network access, downloaded models, or other non-mainline dependencies
    slow: tests that exceed the fast-suite budget or depend on heavy third-party work
